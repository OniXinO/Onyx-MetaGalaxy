OMGSettings
{
  // Активний профіль (назва файлу з каталогу Profiles без .cfg)
  activeProfile = default
  // Строгий режим: не створювати маркери для відсутніх паків у GameData
  strictMarkers = true
  // Вмикати фільтрацію ConfigNode для вимкнених паків
  filterDisabledPacks = true
  // Безпечний режим фільтрації: лише логувати, без видалення
  filterSafeMode = true
}