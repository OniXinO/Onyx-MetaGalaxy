# CHANGELOG

## 0.0.0.3
- Додано фільтрацію `ConfigNode` у плагіні з опціями:
  - `filterDisabledPacks` — увімкнення фільтрації для вимкнених паків.
  - `filterSafeMode` — безпечний режим (лише логування кандидатів).
- Оновлено `README.md` та `Plugins/README.md` з описом нових налаштувань.
- Додано профілі `gu_bh_opm` та `ksrss_gpp`.
- Авто-очищення легасі маркерів у корені `GameData` в скрипті активації.
- Заповнено `URL` та `DOWNLOAD` у `OMG.version` (GitHub).
- Збільшено `BUILD` до 3 у `OMG.version`.